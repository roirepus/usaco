#include <bits/stdc++.h>
using namespace std;
typedef vector<int> vint;

int main(int argc, char *argv[]) {
  int n, k;
  cin >> n >> k;
  string s;
  vint v;
  while (cin >> s) {
    v.push_back(s);
  }

  return 0;
}
