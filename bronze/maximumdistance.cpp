#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
  int n;
  int arrX[n] = {0};
  for (int i = 0; i < n; ++i) {
    cin >> arrX[i];
  }
  int arrY[n] = {0};
  for (int i = 0; i < n; ++i) {
    cin >> arrY[i];
  }

  return 0;
}
